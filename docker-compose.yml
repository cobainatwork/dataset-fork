services:
  easy-dataset:
    image: easy-dataset:local
    container_name: easy-dataset
    env_file: .env
    ports:
      - '1717:1717'
    volumes:
      - ./local-db:/app/local-db
      - ./prisma:/app/prisma
    restart: unless-stopped
